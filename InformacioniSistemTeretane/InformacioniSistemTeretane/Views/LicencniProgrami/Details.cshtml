@model InformacioniSistemTeretane.Models.LicencniProgram

@{
    ViewData["Title"] = "Detalji programa";
    bool imaLicenci = Model.Licence?.Any() ?? false;
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-4">Detalji programa</h1>
        <a asp-action="Index" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Nazad
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-header bg-info text-white">
            <h5 class="mb-0">@Model.Naziv</h5>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Naziv)</dt>
                <dd class="col-sm-8">@Model.Naziv</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Opis)</dt>
                <dd class="col-sm-8">@Model.Opis</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(m => m.TrajanjeDana)</dt>
                <dd class="col-sm-8">@Model.TrajanjeDana dana</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Cijena)</dt>
                <dd class="col-sm-8">@Model.Cijena.ToString("C")</dd>

                <dt class="col-sm-4">Broj licenci</dt>
                <dd class="col-sm-8">@(Model.Licence?.Count ?? 0)</dd>
            </dl>
        </div>
        <div class="card-footer bg-light">
            <div class="d-flex justify-content-end">
                @if (User.IsInRole("Admin") || User.IsInRole("Zaposlenik"))
                {
                    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning me-2">
                        <i class="fas fa-edit"></i> Uredi
                    </a>
                }
                @if (User.IsInRole("Admin"))
                {
                    <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger">
                        <i class="fas fa-trash-alt"></i> Obriši
                    </a>
                }
            </div>
        </div>
    </div>

    @if (imaLicenci)
    {
        <div class="card shadow-sm mt-4">
            <div class="card-header bg-secondary text-white">
                <h5 class="mb-0">Licence za ovaj program</h5>
            </div>
            <div class="card-body">
                <ul class="list-group">
                    @foreach (var licenca in Model.Licence)
                    {
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                <strong>@licenca.Klijent?.Prezime, @licenca.Klijent?.Ime</strong>
                                <div class="text-muted">
                                    @licenca.DatumIzdavanja.ToShortDateString() - @licenca.ValidnaDo.ToShortDateString()
                                </div>
                            </div>
                            <span class="badge @(licenca.ValidnaDo < DateTime.Now ? "bg-secondary" : "bg-success")">
                                @(licenca.ValidnaDo < DateTime.Now ? "Istekla" : "Aktivna")
                            </span>
                        </li>
                    }
                </ul>
            </div>
        </div>
    }
</div>